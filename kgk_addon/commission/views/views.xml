<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="commission.list">
      <field name="name">commission list</field>
      <field name="model">commission.commission</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="commission.action_window">
      <field name="name">commission window</field>
      <field name="res_model">commission.commission</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="commission.action_server">
      <field name="name">commission server</field>
      <field name="model_id" ref="model_commission_commission"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="commission" id="commission.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="commission.menu_1" parent="commission.menu_root"/>
    <menuitem name="Menu 2" id="commission.menu_2" parent="commission.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="commission.menu_1_list" parent="commission.menu_1"
              action="commission.action_window"/>
    <menuitem name="Server to list" id="commission" parent="commission.menu_2"
              action="commission.action_server"/>
    -->
  </data>
</odoo>